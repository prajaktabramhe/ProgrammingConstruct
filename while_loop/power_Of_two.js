function powerOfTwo(NUMBER){
    for (let index=0; index<=limit; index++) {
        let power = (Math.pow(NUMBER, index));
    if(power < 256 ){
        console.log(power)
    }
}
   }
   const NUMBER = 2;
   let limit = 12;
   powerOfTwo(NUMBER);
